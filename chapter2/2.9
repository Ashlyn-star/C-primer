解释下列定义的含义，对于非法的定义，请说明错在何处并将其改正。

(a) std::cin >> int input_value;
非法：输入符号后面不可以定义，必须先定义了变量再输入
(b) int i = { 3.14 };
会报错，使用花括号的方式面对信息遗失会报错
(c) double salary = wage = 9999.99;
非法：同一语句里初始化要分开赋值，但是对于同义语句中后一个对象可以由前一个赋值
(d) int i = 3.14;
正确，i=3