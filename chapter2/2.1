类型 int、long、long long 和 short 的区别是什么？无符号类型和带符号类型的区别是什么？float 和 double的区别是什么？

三者的类型决定了存储空间的大小不一样，一个int至少和一个short一样大，一个long至少和一个int一样大，一个long long至少和一个long一样大。
每个的最小尺寸分别为：short（16位）；int（16位）；long（32位）；long long（64位）。

无符号类型只能表示大于等于0 的数值，带符号类型可以表示负数，对于同数据类型的（如同interesting）的无符号和有符号，无符号能表示的数据更大一些。

float是单精度浮点数，有效精度6位，double双精度，有效十位。
